<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.blueeleven.viewdrawdemo">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".linearlayout.TestLinearActivity" />
    <activity android:name=".linearlayout.activity.TestCustomLinearActivity" />
    <activity android:name=".NativeLinearLayoutActivity" />
    <activity android:name=".linearlayout.activity.NormalLinearActivity" />
    <activity android:name=".linearlayout.activity.WidthWrapLinearActivity" />
    <activity android:name=".linearlayout.activity.WeightLinearActivity" />
    <activity android:name=".draw.activity.PaintActivity1" />
    <activity android:name=".draw.TestDrawActivity" />
    <activity android:name=".draw.activity.ClockViewActivity" />
    <activity android:name=".draw.activity.CobwebViewActivity" />
    <activity android:name=".draw.activity.PathEffectActivity" />
    <activity android:name=".draw.activity.PathActivity" />
  </application>

</manifest>